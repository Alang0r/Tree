package main

import (
	// api "Tree/informer/api"
	// "Tree/informer/models"
	// lib "Tree/lib/service"
	// "sync"
	// "time"
)

func main() {

	// wg := sync.WaitGroup{}
	// srv := lib.Service{}
	// srv.SetName("Tester")

	// srv.Configure()
	// wg.Add(1)
	// go srv.Start()

	// req := api.ReqPersonCreate{}
	// req.Name = "olololo"
	// req.Person = models.Person{
	// 	FirstName: "keke",
	// }
	// req.RabbitChannel = srv.RabbitChannel
	// req.Person.FirstName = "Alex"
	// req.To = "Informer"
	// time.Sleep( 3 * time.Second)
	// req.Send()
	// srv.Log.Info("Request sended")
	// wg.Wait()
}